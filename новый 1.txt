git config user.email "mkden@yandex.ru"
git config user.name "mkden"



git remote add origin https://github.com/mkden/clicaid-app.git
git branch -M main
git push -u origin main



git add config.xml
git add package.json
git add package-lock.json
git add .nvmrc
git commit -m "fix"
git push origin main



git add config.xml
git commit -m "chore: minimal valid config.xml (fix parse error)"
git push

cd C:\Users\User\clicaid
openssl x509 -in distribution.cer -inform DER -out certificate.pem -outform PEM
openssl pkcs12 -export -inkey private.key -in certificate.pem -out certificate.p12




cordova build android --release -- --keystore=android.keystore --storePassword=mortal32323 --alias=aliasf --password=mortal32323


//apk
cordova build android --release --buildConfig=build.json
